<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Administración" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-screen overflow-y-scroll"> 

<header class="bg-gray-800 text-white border-b-4 pb-2 border-sky-400">
  <div class="container mx-auto flex items-center flex-wrap py-4 pl-2 gap-x-4 min-w-0">
    
    <!-- Logo y título alineados a la izquierda -->
    <div class="flex items-center space-x-2 min-w-0">
      <%= link_to admin_root_path do %>
        <%= image_tag('avivasblue.png', alt: 'AVIVAS logo', class: 'inline-block h-12 sm:h-16') %>
        <%= image_tag('admin.png', alt: 'AVIVAS logo', class: 'inline-block h-6 sm:h-6') %>
      <% end %>
    </div>

    <!-- Menú de usuario alineado completamente a la derecha -->
    <div class="flex items-center space-x-2 ml-auto mr-2">
      <%= link_to "Editar perfil", edit_admin_user_path(current_user), 
            class: "text-center hover:text-sky-500 text-white font-medium border-2 border-sky-500 p-1 px-2 rounded-lg text-xs sm:text-sm" %>
      <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, 
            class: "hover:text-red-500 text-white font-medium border-2 border-red-500 p-1 rounded-lg text-xs sm:text-sm" %>
    </div>

  </div>
</header>



    <!-- Contenido -->
    <main class="container mx-auto mt-8 px-6 flex-1">
      <%= render 'shared/alerts' %>
      <%= yield %>
      <br>
    </main>

  </body>
</html>
